---
interface Props{
    title: string;
    body: string;
    link: string;
}


export function horizontalCard(props: Props) {
    return (
        <div class="text-5xl">
            <h3>{props.title}</h3>
            <p>{props.body}</p>
            <a href={props.link}>Read More</a>
        </div>
    )
}

---